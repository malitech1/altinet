"""Service for messaging between Altinet nodes."""

class MessagingService:
    """Placeholder messaging interface."""

    def send(self, recipient: str, payload: bytes) -> None:
        """Send a message to a recipient node."""
        raise NotImplementedError
